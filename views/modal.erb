
<!-- HILLARY MODAL -->

<div class="modal fade" id="hillaryModal" tabindex="-1" role="dialog" aria-labelledby="hillaryModalLabel">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title" id="hillaryModalLabel">Nickname Hillary</h4>
      </div>
      <div class="modal-body">

<!-- HILLARY NAME FORM -->

        <input type="text" class="form-control" placeholder="Add Yours" id="newhname" value="">

        <table class="table table-hover" id="hillary-table">
          <thead>
            <tr>
              <th>Last Updated</th>
              <th>Nickname</th>
              <th>Actions</th>
            </tr>
          </thead>

          <form action="/hillarynames" method="POST" id="formhillary">
          <tbody>
            <% if @hnames != nil %>
              <% @hnames.each do |hname| %>
              <tr>
                <td>
                  <%= hname.updated_at.strftime("%m.%d.%Y") %>
                </td>

                <td>
                  <input name="hillary_name" value="<%= hname.name %>" readonly onfocus="this.value = this.value;" class="highlight">
                </td>

                <td>
                    <div class="formactions">
                      <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                      <input class="delete" type="submit" data-id="<%= hname.id %>" value="❌" style="background-color: white;">
                    </div>
                </td>
              </tr>
              <% end %>
            <% end %>
          </tbody>
          </table>
        <div class="modal-footer">
          <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
          <button type="submit" class="btn btn-primary">Save changes</button>
        </div>
      </form>
      </div>
    </div>
  </div>
</div>




<!-- BERNIE MODAL -->

<div class="modal fade" id="bernieModal" tabindex="-1" role="dialog" aria-labelledby="bernieModalLabel">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title" id="bernieModalLabel">Nickname Bernie</h4>
      </div>
      <div class="modal-body">

  <!-- BERNIE NAME FORM -->

        <input type="text" class="form-control" placeholder="Add Yours" id="newbname" value="">

        <table class="table table-hover" id="bernie-table">
          <thead>
            <tr>
              <th>Last Updated</th>
              <th>Nickname</th>
              <th>Actions</th>
            </tr>
          </thead>

          <form action="/bernienames" method="POST" id="formbernie">
          <tbody>
            <% if @bnames != nil %>
              <% @bnames.each do |bname| %>
              <tr>
                <td>
                  <%= bname.updated_at.strftime("%m.%d.%Y") %>
                </td>

                <td>
                  <input name="bernie_name" value="<%= bname.name %>" readonly onfocus="this.value = this.value;" class="highlight">
                </td>

                <td>
                    <div class="formactions">
                      <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                      <input class="delete" type="submit" data-id="<%= bname.id %>" value="❌" style="background-color: white;">
                    </div>
                </td>
              </tr>
              <% end %>
            <% end %>
          </tbody>
          </table>
        <div class="modal-footer">
          <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
          <button type="submit" class="btn btn-primary">Save changes</button>
        </div>
      </form>
      </div>
    </div>
  </div>
</div>
